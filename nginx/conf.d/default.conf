# Catch-all for unmatched vhosts. NOTE: default_server lives in wordpress.conf
# (that server block must win for real traffic), so this block is plain `listen 80`.
server {
  listen 80;
  server_name _;
  return 444;
}
